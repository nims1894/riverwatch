# CAB-092 · Voyage Track Alignment + Label Meta Grammar

- Latest Snapshot `Market / Plan / Gap` is forced to one horizontal `Label Value` row, matching Voyage Log.
- Market Peak is arranged as four visually even zones. The first zone keeps `MARKET PEAK` and `(ALL TIME)` on two lines; Peak / Date / Gap remain one line.
- Peak field typography is responsive inside its fixed zone to preserve headroom for future larger `M` values without changing the Voyage Track plot area.
- Parenthetical label metadata uses one shared grammar for spacing, size, weight, muted tone, and baseline.
- `PORTFOLIO (ALL ASSETS) · KRW` changed to `PORTFOLIO · ALL ASSETS · KRW`.
- No chart scale, plot area, source data, ETA logic, Market Peak calculation, or business logic was changed.
