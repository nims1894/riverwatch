# CAB-090 TIME LEFT / ETA Label Metadata Layout

- TIME LEFT: `TIME LEFT (YYYY.MM.DD)` / `N,NNN days`
- ETA: `ETA (YYYY.MM.DD, +Δd)` / `N,NNN days`
- Date and deviation remain secondary typography on the label row.
- Existing Target Date, ETA, ETA deviation, and Voyage Health calculation logic are unchanged.
- Card dimensions and Summary layout are unchanged.
- Mobile typography is compacted only for secondary label metadata.
