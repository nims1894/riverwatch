# CAB-019a_CHANGELOG

## RiverWatch v1.0 RC2i — Health Visual Polish Completion

### Fixed
- Voyage Health status now uses centralized CAB-018 status table through `getVoyageStatus()`.
- Removed duplicate score suffix from Voyage Health status text.
- Summary status rows strip score suffix if any legacy value remains.
- `Gap to Destination` and `ETA Extension` subtle cards now match the standard Voyage metric card background.

### Validation
- JavaScript syntax check passed.
